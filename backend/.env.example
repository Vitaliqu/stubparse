ANTHROPIC_API_KEY=your_api_key_here

# Shared secret — must match API_SECRET in frontend env
API_SECRET=change-me-to-a-random-string

# Restrict CORS to your frontend domain (recommended in production)
ALLOWED_ORIGIN=https://stubparse.com
