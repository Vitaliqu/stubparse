# Backend URL — server-side only (no NEXT_PUBLIC_)
BACKEND_URL=http://localhost:8000

# Shared secret — must match API_SECRET in backend env
API_SECRET=change-me-to-a-random-string

# Your site's public URL (used for OG tags and sitemap)
NEXT_PUBLIC_BASE_URL=http://localhost:3000
